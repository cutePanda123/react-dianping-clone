import createReducer from '../../../utils/createReducer';

export const schema = {
    name: 'comments',
    id: 'id'
};

const reducer = (state = {}, action) => {
    return state;
};

export default reducer;